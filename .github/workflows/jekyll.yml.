# This file is intentionally not a GitHub Actions workflow.
# The trailing dot in its name (`jekyll.yml.`) prevents GitHub Actions from loading it.
# Keep the real Pages workflow in `.github/workflows/jekyll.yml` as the single source of truth.
